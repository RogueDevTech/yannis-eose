/**
 * Default accent for campaign order forms (builder + Edge-hosted HTML).
 * Keep aligned with `brand.500` in `packages/config/tailwind.config.ts` (#1565C0).
 */
export const DEFAULT_CAMPAIGN_FORM_ACCENT_HEX = '#1565C0' as const;
